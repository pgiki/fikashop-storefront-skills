{
  "name": "fikashop-storefront-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck:examples": "tsc -p tsconfig.examples.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
